import 'package:flutter/material.dart';

class RiwayatAbsenPage extends StatelessWidget {
  const RiwayatAbsenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Riwayat Absen"));
  }
}
